<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <title>{{ blog_name }}</title>
    <subtitle>{{ blog_name }}</subtitle>
    <updated>{{ articlesList[0].datetime }}</updated>
    <author>
        <name><![CDATA[ {{ blog_author }} ]]></name>
    </author>
    <link href="http://{{ request.host }}" />
    <id>http://{{ request.host }}</id>
    {% for article in articlesList %}
        <entry>
            <title>{{ article.title }}</title>
            <link>http://{{ request.host }}/article/{{ article.id }}</link>
            <id>http://{{ request.host }}/article/{{ article.id }}</id>
            <published>{{ article.datetime }}</published>
            <content type="html">
                <![CDATA[{{ handler.md_to_html(article.content) }}]]>
            </content>
        </entry>
    {% end %}
</feed>